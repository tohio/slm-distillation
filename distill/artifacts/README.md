# Artifact Handoff

Packages trained model outputs for durable storage and server handoff.

The default configuration includes:

- response-distilled checkpoints
- DPO-aligned checkpoints
- evaluation results
- the generated model card

Published training datasets are not duplicated in model artifact bundles.
