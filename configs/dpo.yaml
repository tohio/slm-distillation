
source:
  model_name: smollm2-135m-deepseek-distilled
  checkpoint_path: runs/smollm2-135m-deepseek-distilled/response_distill/checkpoints/final
  tokenizer_path: runs/smollm2-135m-deepseek-distilled/response_distill/checkpoints/final
  revision: null

data:
  dataset_id: tohio/slm-synthetic-distillation-dpo
  dataset_config_name: null
  dataset_split: train
  id_field: id
  prompt_field: prompt
  chosen_field: chosen
  rejected_field: rejected
  metadata_field: metadata

training:
  method: dpo
  beta: 0.1
  max_length: 1024
  per_device_train_batch_size: 1
  gradient_accumulation_steps: 8
  learning_rate: 0.000005
  num_train_epochs: 1
  warmup_ratio: 0.03
  weight_decay: 0.0
  max_grad_norm: 1.0
  lr_scheduler_type: cosine
  logging_steps: 10
  save_steps: 250
  save_total_limit: 2
  bf16: true
  gradient_checkpointing: true
  dataloader_num_workers: 4
  dataset_num_proc: 4
  loss_type: sigmoid
  truncation_mode: keep_start
  precompute_ref_log_probs: false
  seed: 42
  max_steps: null
  max_train_samples: null

output:
  model_name: smollm2-135m-deepseek-distilled
  run_dir: runs/smollm2-135m-deepseek-distilled/dpo
  checkpoint_dir: runs/smollm2-135m-deepseek-distilled/dpo/checkpoints
  final_checkpoint_dir: runs/smollm2-135m-deepseek-distilled/dpo/checkpoints/final
