# Rejected Teacher Outputs

Teacher responses rejected during validation.

Generated by:

```bash
make validate
```

Rejected records are useful for debugging prompt quality, provider behavior, validation rules, and retry policy.

Do not delete rejected outputs until the run has been reviewed.
